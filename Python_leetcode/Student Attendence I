class Solution:
    def checkRecord(self, s: str) -> bool:
        absent = s.count("A")
        if absent >= 2:
            return False
        if "LLL" in s:
            return False
        return True
