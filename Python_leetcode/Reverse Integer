class Solution:
    def reverse(self, x: int) -> int:
        if x < 0:
            reversed_x = int('-' + str(x)[:0:-1])
        else:
            reversed_x = int(str(x)[::-1])

        if -2147483648 <= reversed_x <= 2147483647:
            return reversed_x
        else:
            return 0
