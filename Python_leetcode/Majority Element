from collections import Counter
class Solution:
    def majorityElement(self, nums: List[int]) -> int:
        x = Counter(nums)
        for key, value in x.items():
            if value > len(nums) // 2:
                return key        
