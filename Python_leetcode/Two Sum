class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        numarr = {}
        for i, num in enumerate(nums):
            comp = target - num
            if comp in numarr:
                return [numarr[comp],i]
            else:
                numarr[num] = i
        return
            
